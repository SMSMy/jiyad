# دليل تعديل مشروع Seal إلى تطبيق جياد المبسط

## 🎯 الهدف العام

تحويل مشروع Seal (تطبيق تحميل معقد) إلى تطبيق "جياد" بسيط ومخصص لكبار السن، مع الحفاظ على إمكانية دمج التحديثات المستقبلية من المشروع الأصلي بدون تعقيدات.

---

## 📋 المبدأ الأساسي: "Fork الجراحي"

**القاعدة الذهبية:**

- ✅ **لا تعدل ملفات Seal الأصلية** - أنشئ ملفاتك الجديدة
- ✅ **اعزل كودك تماماً** - ضع كل شيء في مجلد `simple/`
- ✅ **التعديل الأدنى** - غيّر فقط نقطة الدخول (MainActivity)

**لماذا؟**

- تجنب تضارب الدمج (Merge Conflicts) عند تحديث من upstream
- صيانة سهلة (2-3 ساعات سنوياً بدلاً من 50+)
- كود نظيف ومفهوم

---

## 🗂️ بنية المشروع الحالية

```
jiyad/
├── app/src/main/java/com/junkfood/seal/
│   ├── MainActivity.kt              ← سنعدل سطراً واحداً فقط
│   ├── ui/page/download/            ← واجهات Seal المعقدة (لا نمسها)
│   ├── ui/page/settings/            ← إعدادات معقدة (لا نمسها)
│   ├── download/DownloaderV2.kt     ← محرك التحميل (لا نمسه)
│   └── database/                    ← قاعدة بيانات (لا نمسها)
│
├── app/src/main/res/
│   ├── mipmap-*/ic_launcher.png     ← الأيقونات الحالية (سنستبدلها)
│   └── values/strings.xml           ← النصوص (سنضيف نصوصنا)
│
└── C:\Code\jiyad\jiyad\ico.png      ← الأيقونة الجديدة (خارج المشروع حالياً)
```

---

## ✅ المهام المطلوبة بالتفصيل

### المرحلة 1: إنشاء البنية المعزولة

#### 1.1 إنشاء مجلد `simple/`

**الموقع:** `app/src/main/java/com/junkfood/seal/simple/`

**الهدف:** عزل كل كود جياد عن كود Seal

**الملفات التي ستنشئها هنا:**

```
simple/
├── SimpleHomeScreen.kt      ← الواجهة البسيطة
├── JiyadDownloader.kt       ← محرك التحميل المبسط
├── SimpleConfig.kt          ← الإعدادات المُشفرة
└── SimpleViewModel.kt       ← (اختياري) إدارة الحالة
```

---

### المرحلة 2: تطوير الواجهة البسيطة

#### 2.1 إنشاء `SimpleHomeScreen.kt`

**الموقع:** `app/src/main/java/com/junkfood/seal/simple/SimpleHomeScreen.kt`

**الهدف:** واجهة بسيطة للغاية تحتوي على:

- حقل إدخال رابط (كبير، خط واضح)
- زر "تحميل فيديو 🎬" (كبير، 90dp ارتفاع)
- زر "تحميل صوت 🎵" (كبير، 90dp ارتفاع)
- شريط تقدم (عند التحميل)
- رسالة الحالة (نجاح/فشل)

**المتطلبات:**

- استخدام Jetpack Compose
- Material 3 Design
- خطوط كبيرة (24-28sp)
- مسافات واضحة (24dp بين العناصر)
- ألوان متباينة للوضوح

**التكامل:**

- استدعاء `JiyadDownloader` للتحميل
- لا تعتمد على `DownloadViewModel` الخاص بـ Seal
- لا تستخدم Hilt/Dagger (للبساطة)

---

#### 2.2 إنشاء `JiyadDownloader.kt`

**الموقع:** `app/src/main/java/com/junkfood/seal/simple/JiyadDownloader.kt`

**الهدف:** طبقة تحميل بسيطة تستخدم `YoutubeDL` مباشرة

**الوظائف المطلوبة:**

```kotlin
object JiyadDownloader {
    suspend fun downloadVideo(context, url, onProgress, onComplete)
    suspend fun downloadAudio(context, url, onProgress, onComplete)
    suspend fun updateYtDlp(context): Boolean
    private fun getDownloadPath(context): String
}
```

**الإعدادات المُشفرة (Hardcoded):**

- فيديو: `bestvideo[height<=720]+bestaudio/best` (جودة 720p)
- صوت: `bestaudio/best --extract-audio --audio-format mp3`
- مجلد التحميل: `Downloads/جياد`
- استخدام aria2c للتحميل السريع

**المزايا:**

- لا اعتمادية على Seal infrastructure
- لا Hilt، لا Room، لا WorkManager
- كود بسيط (150 سطر فقط)
- صيانة سهلة

---

#### 2.3 إنشاء `SimpleConfig.kt`

**الموقع:** `app/src/main/java/com/junkfood/seal/simple/SimpleConfig.kt`

**الهدف:** تجميع الإعدادات الثابتة في مكان واحد

**المحتوى:**

```kotlin
object SimpleConfig {
    const val VIDEO_FORMAT = "bestvideo[height<=720]+bestaudio/best"
    const val AUDIO_FORMAT = "bestaudio/best"
    const val DOWNLOAD_DIR_NAME = "جياد"
    const val AUTO_UPDATE_YTDLP = true
    const val USE_ARIA2C = true
}
```

---

### المرحلة 3: تعديل نقطة الدخول

#### 3.1 تعديل `MainActivity.kt`

**الموقع:** `app/src/main/java/com/junkfood/seal/MainActivity.kt`

**التعديل الوحيد المطلوب:**

**ابحث عن:**

```kotlin
setContent {
    SealTheme {
        AppEntry()  // أو HomeScreen() أو ما شابه
    }
}
```

**استبدل بـ:**

```kotlin
setContent {
    SealTheme {
        SimpleHomeScreen()  // واجهة جياد البسيطة
    }
}
```

**ملاحظة مهمة:**

- هذا التعديل الوحيد على ملفات Seal
- احتمالية conflict عند merge: 5% فقط
- سهل الحل (اختيار نسختك من السطر)
- تأكد من أن build.gradle.kts يحتوي على implementation("com.github.yausername.youtubedl-android:library:...") وإذا لم يكن موجوداً (لأن Seal يستخدم نسخة مخصصة)، وجهني لكيفية استدعاء مكتبة Seal الداخلية.
- في SimpleHomeScreen.kt، أضف منطقاً بسيطاً لطلب إذن التخزين (Storage Permission) إذا كان إصدار الأندرويد أقل من 10، قبل بدء التحميل

---

### المرحلة 4: استبدال الأيقونة

#### 4.1 فهم نظام الأيقونات في Android

Android يحتاج أيقونات بدقات مختلفة:

- `mipmap-mdpi`: 48×48px
- `mipmap-hdpi`: 72×72px
- `mipmap-xhdpi`: 96×96px
- `mipmap-xxhdpi`: 144×144px
- `mipmap-xxxhdpi`: 192×192px

**الملفات الحالية:**

```
app/src/main/res/
├── mipmap-hdpi/ic_launcher.png
├── mipmap-mdpi/ic_launcher.png
├── mipmap-xhdpi/ic_launcher.png
├── mipmap-xxhdpi/ic_launcher.png
└── mipmap-xxxhdpi/ic_launcher.png
```

#### 4.2 تحويل `ico.png` إلى جميع الأحجام

**الأيقونة الجديدة:** `C:\Code\jiyad\jiyad\ico.png`

**الخطوات المطلوبة:**

1. **نقل الأيقونة للمشروع:**

   ```
   من: C:\Code\jiyad\jiyad\ico.png
   إلى: jiyad/app/src/main/res/drawable/original_icon.png
   ```

2. **توليد الأحجام المختلفة:**
   - استخدم Android Studio: Right-click على `res` → New → Image Asset
   - أو استخدم أداة online: https://icon.kitchen
   - أو استخدم ImageMagick (command line)

3. **استبدال الملفات:**

   ```
   احذف:
   - app/src/main/res/mipmap-*/ic_launcher.png
   - app/src/main/res/mipmap-*/ic_launcher_foreground.png
   - app/src/main/res/mipmap-*/ic_launcher_round.png

   ضع الجديد:
   - app/src/main/res/mipmap-mdpi/ic_launcher.png (48×48)
   - app/src/main/res/mipmap-hdpi/ic_launcher.png (72×72)
   - app/src/main/res/mipmap-xhdpi/ic_launcher.png (96×96)
   - app/src/main/res/mipmap-xxhdpi/ic_launcher.png (144×144)
   - app/src/main/res/mipmap-xxxhdpi/ic_launcher.png (192×192)
   ```

4. **تحديث اسم التطبيق:**
   **الموقع:** `app/src/main/res/values/strings.xml`

   **ابحث عن:**

   ```xml
   <string name="app_name">Seal</string>
   ```

   **استبدل بـ:**

   ```xml
   <string name="app_name">جياد</string>
   ```

5. **تحديث package name (اختياري لكن موصى به):**
   **الموقع:** `app/build.gradle.kts`

   **ابحث عن:**

   ```kotlin
   applicationId = "com.junkfood.seal"
   ```

   **استبدل بـ:**

   ```kotlin
   applicationId = "com.jiyad.downloader"
   ```

---

### المرحلة 5: إضافة نصوص عربية

#### 5.1 إضافة strings.xml للعربية

**الموقع:** `app/src/main/res/values-ar/strings.xml`

**إذا لم يكن موجوداً، أنشئه:**

```xml
<?xml version="1.0" encoding="utf-8"?>
<resources>
    <string name="app_name">جياد</string>
    <string name="download_video">تحميل فيديو 🎬</string>
    <string name="download_audio">تحميل صوت 🎵</string>
    <string name="paste_url">الصق الرابط هنا</string>
    <string name="downloading">جاري التحميل...</string>
    <string name="download_success">تم التحميل بنجاح ✅</string>
    <string name="download_failed">فشل التحميل ❌</string>
    <string name="enter_url">أدخل رابط يوتيوب</string>
</resources>
```

---

### المرحلة 6: تفعيل التحديث التلقائي لـ yt-dlp

#### 6.1 إضافة تحديث عند بدء التطبيق

**الموقع:** `SimpleHomeScreen.kt`

**الكود:**

```kotlin
LaunchedEffect(Unit) {
    // تحديث yt-dlp تلقائياً عند فتح التطبيق
    JiyadDownloader.updateYtDlp(context)
}
```

**الهدف:**

- تحديث yt-dlp في الخلفية عند كل فتح للتطبيق
- لا يتطلب تدخل المستخدم
- يحل 80% من مشاكل التوافق مع YouTube

---

### المرحلة 7: البناء والاختبار

#### 7.1 البناء الأولي

```bash
# في terminal داخل مجلد المشروع:
./gradlew assembleDebug
```

**المتوقع:**

- Build successful
- APK في: `app/build/outputs/apk/debug/app-debug.apk`
- الحجم: 20-25 MB (بسبب yt-dlp + aria2c)

#### 7.2 الاختبار على هاتف

```bash
# توصيل الهاتف عبر USB + تفعيل USB Debugging
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

**سيناريوهات الاختبار:**

1. ✅ فتح التطبيق → الأيقونة الجديدة ظاهرة
2. ✅ الاسم "جياد" في قائمة التطبيقات
3. ✅ الواجهة بسيطة (حقل + زرين)
4. ✅ نسخ رابط يوتيوب
5. ✅ الضغط على "تحميل فيديو"
6. ✅ شريط التقدم يعمل
7. ✅ رسالة "تم التحميل بنجاح"
8. ✅ الملف موجود في `Downloads/جياد`
9. ✅ اختبار "تحميل صوت" بنفس الطريقة

---

## 🔄 استراتيجية الصيانة المستقبلية

### كيف تدمج تحديثات Seal (كل شهرين)

```bash
# 1. جلب التحديثات
git fetch upstream

# 2. دمجها
git merge upstream/main

# 3. إذا ظهر conflict (نادر جداً):
# افتح الملف → ابحث عن <<<<<
# اختر النسخة التي تريدها
git add .
git commit

# 4. البناء والاختبار
./gradlew assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

**الوقت المتوقع:**

- بدون conflicts: 5 دقائق
- مع conflict بسيط: 15 دقيقة
- **الإجمالي السنوي: 2-3 ساعات فقط**

---

## ⚠️ الأخطاء الشائعة وتجنبها

### ❌ لا تفعل:

1. ❌ تعديل `DownloadViewModel.kt` الخاص بـ Seal
2. ❌ تعديل `DownloaderV2.kt`
3. ❌ حذف ملفات Seal (اتركها كما هي)
4. ❌ تعديل `build.gradle.kts` إلا للضرورة
5. ❌ الاعتماد على Hilt في كودك

### ✅ افعل:

1. ✅ أنشئ ملفاتك في `simple/`
2. ✅ استخدم `YoutubeDL` مباشرة
3. ✅ اجعل الإعدادات hardcoded
4. ✅ اختبر بعد كل تغيير
5. ✅ اعمل commit بعد كل خطوة ناجحة

---

## 📊 قائمة التحقق النهائية

قبل اعتبار المشروع جاهزاً:

- [ ] مجلد `simple/` موجود ويحتوي على الملفات الثلاثة
- [ ] `SimpleHomeScreen.kt` يعمل بدون أخطاء
- [ ] `JiyadDownloader.kt` يحمّل الفيديو بنجاح
- [ ] `JiyadDownloader.kt` يحمّل الصوت بنجاح
- [ ] `MainActivity.kt` معدل (سطر واحد فقط)
- [ ] الأيقونة الجديدة في جميع مجلدات mipmap
- [ ] اسم التطبيق "جياد" في strings.xml
- [ ] النصوص العربية موجودة في values-ar
- [ ] البناء ناجح (Build successful)
- [ ] التطبيق يعمل على هاتف حقيقي
- [ ] التحميل فعلاً يعمل (فيديو + صوت)
- [ ] الملفات تُحفظ في `Downloads/جياد`
- [ ] التحديث التلقائي لـ yt-dlp يعمل

---

## 🎓 الخلاصة

**ما تم تحقيقه:**

- ✅ تطبيق بسيط لكبار السن (واجهة نظيفة)
- ✅ معزول تماماً عن كود Seal (صيانة سهلة)
- ✅ تحديث تلقائي لـ yt-dlp (يحل معظم المشاكل)
- ✅ تكلفة صيانة: 2-3 ساعات سنوياً
- ✅ أيقونة واسم مخصص

**الملفات المعدلة من Seal:**

1. `MainActivity.kt` (سطر واحد)
2. `res/values/strings.xml` (اسم التطبيق)
3. `res/mipmap-*/` (الأيقونات)

**الملفات الجديدة (معزولة تماماً):**

1. `simple/SimpleHomeScreen.kt`
2. `simple/JiyadDownloader.kt`
3. `simple/SimpleConfig.kt`
4. `res/values-ar/strings.xml`

---

**الآن ابدأ التنفيذ خطوة بخطوة! 🚀**
